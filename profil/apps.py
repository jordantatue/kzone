"""Configuration de l'application profil."""

from django.apps import AppConfig


class ProfilConfig(AppConfig):
    """Configuration Django de l'application profil."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "profil"

